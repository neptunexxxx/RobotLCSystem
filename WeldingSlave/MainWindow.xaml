<Window x:Class="WeldingSlave.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="智能焊接系统下位机" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="智能焊接系统下位机UDP通讯程序" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- 配置区域 -->
        <GroupBox Grid.Row="1" Header="连接配置" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="上位机IP:" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         x:Name="txtHostIP" 
                         VerticalAlignment="Center"
                         Height="25"/>

                <TextBlock Grid.Row="0" Grid.Column="3" 
                           Text="子系统类型:" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox Grid.Row="0" Grid.Column="4" 
                          x:Name="cmbSubSystem" 
                          VerticalAlignment="Center"
                          Height="25"/>
            </Grid>
        </GroupBox>

        <!-- 控制按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,10">
            <Button x:Name="btnStart" 
                    Content="启动下位机" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="BtnStart_Click"/>
            <Button x:Name="btnStop" 
                    Content="停止下位机" 
                    Width="100" 
                    Height="35"
                    Click="BtnStop_Click"/>
        </StackPanel>

        <!-- 通讯日志 -->
        <GroupBox Grid.Row="3" Header="通讯日志" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="txtLog" 
                         IsReadOnly="True" 
                         Background="Black" 
                         Foreground="Lime" 
                         FontFamily="Consolas" 
                         FontSize="12"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"/>
            </ScrollViewer>
        </GroupBox>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <Label x:Name="lblStatus" Content="状态: 未启动" FontWeight="Bold"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>